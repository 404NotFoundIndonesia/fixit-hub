<% if object.errors.any? %>
  <div class="form-errors" role="alert">
    <strong><%= pluralize(object.errors.count, "error") %> prevented this form from being saved:</strong>
    <ul>
      <% object.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
